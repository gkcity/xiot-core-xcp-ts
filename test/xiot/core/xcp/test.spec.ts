import { expect } from 'chai';
import 'mocha';
import * as fs from 'async-file';

describe('test', async () => {

    it('test', () => {
        expect(true).to.equal(true);
    });

    expect(true).to.equal(true);
});